# Local pre-commit gates — the same checks CI runs, moved left so drift is caught before a push.
# The Python project lives in backend/, so run pre-commit from the repo root via uv's tool runner
# (config stays here at the repo root, where the git hook reads it):
#   Install once:  uvx pre-commit install     Run on demand:  uvx pre-commit run --all-files
# Hook versions are pinned; ruff-pre-commit is pinned to the same ruff as the dev group.
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-merge-conflict
      - id: check-added-large-files

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.3
    hooks:
      - id: ruff-check
        args: [--fix]
      - id: ruff-format

  # mypy needs the project's own dependencies to resolve imports, so run it through uv in the
  # project env rather than pre-commit's isolated virtualenv. Scoped to the typed packages CI checks.
  - repo: local
    hooks:
      - id: mypy
        name: mypy (app, evaluation)
        # Runs from the repo root; --directory backend runs mypy inside the backend project.
        entry: uv run --directory backend mypy app evaluation
        language: system
        types: [python]
        pass_filenames: false
